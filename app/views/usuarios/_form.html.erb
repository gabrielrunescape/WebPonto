<%= form_for(usuario, class: "form-signin") do |f| %>
  <% if usuario.errors.any? %>
    <div class="alert alert-danger">
      <strong><%= pluralize(usuario.errors.count, "error") %> prohibited this usuario from being saved:</strong>
    </div>

    <% usuario.errors.full_messages.each do |message| %>
      <div class="alert alert-warning">
        <%= message %>
      </div>
    <% end %>
  <% end %>

  <%= f.label :nome, class: "sr-only" %>
  <%= f.text_field :nome, class: "form-control", placeholder: "Nome completo" %>

  <%= f.label :login, class: "sr-only" %>
  <%= f.text_field :login, class: "form-control", placeholder: "Nome de usuário" %>

  <%= f.label :email, class: "sr-only" %>
  <%= f.email_field :email, class: "form-control", placeholder: "E-mail" %>

  <%= f.label :senha, class: "sr-only" %>
  <%= f.password_field :senha, class: "form-control", placeholder: "Senha" %>

  <%= f.submit 'Registrar', :class => "btn btn-lg btn-success btn-block" %>
<% end %>
